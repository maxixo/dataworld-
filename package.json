{
  "name": "dataworld-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix server run build",
    "start":  "npm --prefix server start",
    "dev": "nodemon src/server.ts"
  },
  "engines": {
    "node": "20.x"
  }
}
